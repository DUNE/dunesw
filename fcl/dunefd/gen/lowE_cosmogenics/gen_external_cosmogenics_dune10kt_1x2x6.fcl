# gen_external_cosmogenics_dune10kt_1x2x6.fcl
#
# Generation stage for external cosmogenics in DUNE FD HD 1x2x6.
# "External cosmogenics" are particles (gammas, neutrons, electrons, etc.)
# produced by cosmogenic processes in the surrounding rock/structure and
# entering the detector, pre-simulated by a standalone G4 campaign.
#
# Particles are read from a HEPEVT file produced by external_cosmogenics_to_hepevt.py.
# They are placed at the central APA plane (x = 0), consistent with the RadioGen
# gammas_from_cavernwall_atLAr convention. Directions are NOT isotropic: they are
# drawn from the per-species (cos(theta), phi) distribution held in the sampling
# table, whose azimuth is measurably non-flat (chi2/ndf = 222 for gammas).
#
# This FCL is complementary to gen_internal_cosmogenics_dune10kt_1x2x6.fcl (which generates
# cosmogenic isotopes inside the LAr). Both should be run as separate samples.
#
# Workflow:
#   1. Build the HEPEVT file from the sampling table (see README.md):
#        python external_cosmogenics_to_hepevt.py external_cosmogenics_table.dat external_cosmogenics.hepevt
#   2. Gen stage (run from the directory containing external_cosmogenics.hepevt).
#      -n is REQUIRED -- see the maxEvents comment in the source block below:
#        lar -c gen_external_cosmogenics_dune10kt_1x2x6.fcl \
#            -n $(cat external_cosmogenics.hepevt.nevents) \
#            -o external_cosmogenics_gen.root
#   3. G4 stage:
#        lar -c g4_external_cosmogenics_dune10kt_1x2x6.fcl \
#            -s external_cosmogenics_gen.root -o external_cosmogenics_g4.root

#include "services_dune.fcl"

process_name: ExternalCosmogenicsGen

services:
{
   @table::dunefd_1x2x6_simulation_services
   TFileService:          { fileName: "external_cosmogenics_gen_hist.root" }
   RandomNumberGenerator: {}
}

source:
{
   module_type: EmptyEvent
   timestampPlugin: { plugin_type: "GeneratedEventTimestamp" }
   # MUST be overridden with -n, using the count in <hepevt>.nevents that
   # external_cosmogenics_to_hepevt.py writes beside the HEPEVT file.
   #
   # -1 does NOT mean "read until EOF" here. TextFileGen throws when it runs off
   # the end of the file instead of ending the job, so if -n is omitted art
   # aborts on the last event, never closes RootOutput, and leaves an orphan
   # RootOutput-*.root temp file instead of the -o name.
   #
   # That is kept deliberately, as a fail-closed guard. Any finite default is
   # worse: with -n forgotten it would exit 0 and write a short file that looks
   # valid, silently understating the exposure. A crash with no output cannot be
   # mistaken for a good run. Give -n the sidecar count and this never fires.
   maxEvents:   -1
   firstRun:    20003000
   firstEvent:  1
}

physics:
{
   producers:
   {
      rns:       { module_type: "RandomNumberSaver" }
      generator:
      {
         module_type:   "TextFileGen"
         InputFileName: "external_cosmogenics.hepevt"
         Offset:        0
      }
   }

   simulate:      [ rns, generator ]
   stream1:       [ out1 ]
   trigger_paths: [ simulate ]
   end_paths:     [ stream1 ]
}

outputs:
{
   out1:
   {
      module_type: RootOutput
      fileName:    "external_cosmogenics_gen.root"
      dataTier:    "generated"
      compressionLevel: 1
   }
}
